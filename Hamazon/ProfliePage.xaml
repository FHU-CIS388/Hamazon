<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Hamazon.ProfliePage">
    <ContentPage.Content>

        <ScrollView>

            <Grid RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <!-- Header -->
                <BoxView HeightRequest="200" BackgroundColor="Blue" Grid.Row="0"/>

                <!-- Profile Image-->
                <Image Source="dshannon.jpg" WidthRequest="100" HeightRequest="100" Grid.Row="1"/>

                <!-- Stats-->
                <StackLayout Orientation="Horizontal" Grid.Row="2">
                     <Label Text="3" FontSize="Title" />
                     <Label Text="8" FontSize="Title" />
                     <Label Text="11" FontSize="Title" />
                </StackLayout>

                <!-- Bio-->
                <BoxView HeightRequest="400" BackgroundColor="Orange" Grid.Row="3"/>

                <!-- Bacon Button -->
                <BoxView HeightRequest="50" BackgroundColor="Gray" Grid.Row="4"/>

            </Grid>

        </ScrollView>

    </ContentPage.Content>
</ContentPage>
